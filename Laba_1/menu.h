#pragma once
#pragma once
#include <iostream>
#include <string>

using namespace std;

void menu();
int check(int num, char* arr[]);
string takeName(string& file, string& newfile);
void inputName(char* name, char* newname);